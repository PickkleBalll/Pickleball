const AnalyticsReporting = () => {
  return (

    // Left page
    <div className="analytics-reporting flex">
     <code> Analytics & Reporting</code>
    </div>

  );
};

export default AnalyticsReporting;